[package]
name = "ned"
version = "0.1.10-beta2"
authors = ["Nev Delap <nevdelap@gmail.com>"]

[dependencies]
ansi_term = "0.11.*"
getopts = "0.2.*"
glob = "0.2.*"
libc = "0.2.*"
regex = "0.1.*"
time = "0.1.*"
walkdir = "2.1.*"

[profile.release]
lto = true
