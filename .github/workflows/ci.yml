name: CI

on:
  push:
#   pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-lint-test:
    name: Build, lint, test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    env:
      CARGO_TERM_COLOR: ${{ matrix.os == 'windows-latest' && 'never' || 'always' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Format Markdown
        if: runner.os == 'Linux'
        run: just format_markdown

      - name: Format Rust
        if: runner.os == 'Linux'
        run: just format_rust

      - name: Lint Markdown
        if: runner.os == 'Linux'
        run: just lint_markdown

      - name: Lint Rust
        if: runner.os == 'Linux'
        run: just lint_rust

      - name: Build Debug
        run: just build

      - name: Test Debug
        run: just test

      - name: Build Release
        run: just build --release

      - name: Test Release
        run: just test --release

  cargo-deny:
    name: Dependency policy checks (cargo-deny)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Install cargo-deny
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-deny

      - name: Run cargo-deny checks
        run: |
          cargo deny check

  msrv:
    name: MSRV build, lint, test (ubuntu)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Rust (MSRV)
        uses: dtolnay/rust-toolchain@v1.72.0

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build (MSRV)
        run: cargo build --all-targets

      - name: Clippy (MSRV)
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Test (MSRV)
        run: cargo test

#   man-compare:
#     name: man/compare flag drift check
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Install just
#         uses: taiki-e/install-action@v2
#         with:
#           tool: just

#       - name: Run comparison via just
#         run: just man-compare
